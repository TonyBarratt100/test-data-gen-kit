users:
  columns:
    email:
      provider: internet.email
    full_name:
      provider: person.name
    password:
      provider: password.hash

reviews:
  columns:
    comment:
      provider: text.sentence
