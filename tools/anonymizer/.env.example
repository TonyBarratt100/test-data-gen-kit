# Connection settings for source & masked Postgres DBs

# Postgres connection
PGHOST=127.0.0.1
PGPORT=55433
PGUSER=hackathon_user
PGPASSWORD=hackathon_pass

# Source DB (the seeded dataset from the hackathon)
SRC_DB=hackathon_db

# Destination DB (new anonymized dataset)
DST_DB=hackathon_db_masked

# Optional dry-run limiter (copy only N users/products and related orders/reviews)
# DRY_LIMIT=100
